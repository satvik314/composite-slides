{
  "name": "composite-slides",
  "version": "1.0.0",
  "description": "Presentations where every slide speaks its own visual language",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
